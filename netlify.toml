[build]
  command = "bun run build && cp public/contact.html dist/contact.html && cp public/_redirects dist/_redirects"
  publish = "dist"

[[redirects]]
  from = "/contact"
  to = "/contact.html"
  status = 200
